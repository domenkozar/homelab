diff --git a/drivers/gpu/drm/amd/display/dc/link/protocols/link_dp_capability.c b/drivers/gpu/drm/amd/display/dc/link/protocols/link_dp_capability.c
index 7bdb7905e91a..e5a8f3c12d01 100644
--- a/drivers/gpu/drm/amd/display/dc/link/protocols/link_dp_capability.c
+++ b/drivers/gpu/drm/amd/display/dc/link/protocols/link_dp_capability.c
@@ -1582,6 +1582,15 @@ enum dc_status dp_retrieve_lttpr_cap(struct dc_link *link)
 	if (!vbios_lttpr_interop || !link->dc->caps.extended_aux_timeout_support)
 		return DC_NOT_SUPPORTED;

+	/*
+	 * If LTTPR mode override is set to non-LTTPR, skip reading LTTPR caps
+	 * entirely.  This avoids unreliable AUX reads to LTTPR DPCD registers
+	 * on platforms where the LTTPR is non-functional or causes link
+	 * training failures (e.g. some USB-C monitors with broken LTTPRs).
+	 */
+	if (link->dc->debug.lttpr_mode_override == LTTPR_MODE_NON_LTTPR)
+		return DC_NOT_SUPPORTED;
+
 	/* By reading LTTPR capability, RX assumes that we will enable
 	 * LTTPR extended aux timeout if LTTPR is present.
 	 */
diff --git a/drivers/gpu/drm/amd/display/dc/resource/dcn314/dcn314_resource.c b/drivers/gpu/drm/amd/display/dc/resource/dcn314/dcn314_resource.c
index 4f32229a2e67..b6d1c8e3a0f1 100644
--- a/drivers/gpu/drm/amd/display/dc/resource/dcn314/dcn314_resource.c
+++ b/drivers/gpu/drm/amd/display/dc/resource/dcn314/dcn314_resource.c
@@ -871,6 +871,12 @@ static const struct dc_debug_options debug_defaults_drv = {
 	.disable_z10 = false,
 	.enable_z9_disable_interface = true,
 	.minimum_z8_residency_time = 2100,
+	/*
+	 * Skip LTTPR on DCN 3.1.4 (Phoenix/Hawk Point).  The LTTPR in some
+	 * USB-C paths has an unreliable AUX channel that causes intermittent
+	 * link training failures, green screens, or reduced resolutions.
+	 */
+	.lttpr_mode_override = LTTPR_MODE_NON_LTTPR,
 	.psr_skip_crtc_disable = true,
 	.replay_skip_crtc_disabled = true,
 	.disable_dmcu = true,
